# Ephemeral local working state (see kernel/rules/shared/scratch.md)
**/.scratch/
.DS_Store
# Per-machine secrets / env
.env
.env.*
!.env.example
# Per-machine retro watermark (transcripts live in each machine's ~/.claude)
last-mined-*
# Ephemeral, deployment-specific triage backlog (not part of the published reference)
improvements/
